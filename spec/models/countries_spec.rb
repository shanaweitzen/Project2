require 'spec_helper'

describe Countries do
  it "returns the country id" do
  	expect(subject.retrieveCountry ).to eql(id)
end

it "returns lat and long" do
	expect(subject.coordinates).to eql(Countries name, title) 
end

end
